FROM debian:stretch

LABEL maintainer="InnovativeInventor"

ENTRYPOINT [ "bash" ]

RUN apt-get update && apt-get install -y \
    python3 \
    && rm -rf /var/lib/apt/lists/*
RUN apt-get update && apt-get install -y \
    python3-pip \
    && rm -rf /var/lib/apt/lists/*
