file:
  /root/.pyenv/bin:
    filetype: directory
    exists: true  

command:
  pyenv -h:
    # required attributes
    exit-status: 0
    # optional attributes
    stdout:
    - "Usage: pyenv"
    stderr: []
    timeout: 10000 # in milliseconds
