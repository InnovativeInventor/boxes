command:
  curl -h:
    # required attributes
    exit-status: 0
    # optional attributes
    stdout:
    - "Usage: curl"
    stderr: []
    timeout: 10000 # in milliseconds


command:
  git:
    # required attributes
    exit-status: 0
    # optional attributes
    stdout:
    - "usage: git "
    stderr: []
    timeout: 10000 # in milliseconds

command:
  nano -h:
    # required attributes
    exit-status: 0
    # optional attributes
    stdout:
    - "Usage: nano"
    stderr: []
    timeout: 10000 # in milliseconds

